# => ArgumentError, wrong number of arguments (The given input is not formatted as an iterable, so the method will the elements as extra arguments)
# => NoMethodError? or something like that. (int class does not have each method)
